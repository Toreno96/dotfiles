# vim: set filetype=sh:

# Don't show progress meter; especially useful when piping `curl`
silent
# Disable progress meter but still show error messages
show-error

# Include the HTTP response headers in the output; to make it similar to how
# `httpie` works by default
include
