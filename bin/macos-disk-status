#!/bin/bash

ICON='disk'
PARTITIONS=('/')

PARTITIONS_WITH_SIZE=()
for partition in ${PARTITIONS[*]}; do
    PARTITIONS_WITH_SIZE+=("${partition}:$(df -H "${partition}" | sed -n '2p' | awk '{ print $4 }')")
done

echo "${ICON} ${PARTITIONS_WITH_SIZE[*]}"
